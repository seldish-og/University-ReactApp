
// сделаем так чтобы пользователь сам мог покрасить текст введа hex код
export const Welcome = (props) => {
  // Возвращаем JSX-элемент с использованием props
  return <h1>Hello, {props.name}</h1>;
}